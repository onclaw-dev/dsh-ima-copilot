# Adds the native IMA Copilot tool and its Web settings card to a Harness profile.
- insert:
    - id: ima-copilot
      name: dsh-ima-copilot
      inject: [tools, credentials]
      config:
        requestTimeoutMs: 300000
        retryCount: 3
        concurrencyLimit: 1
